import static org.graalvm.compiler.debug.TTY.printf;

public class Fahrenhet {
    //*Input temperature in degree celcius,
    convert to degree fahrenheit*/
    #include<stido,h>
    main()
    {
        float temp;
        printf("input temperature in degree Celcius") ;
        scanf("%f", &temp);
        printf("equivalent temperature in degree Fahrenheit is %f", temp*1.8+32);

        float temp;
        printf("input temperature in degree Fahrenheit");
        scanf("%c", &temp);
        printf("equivalent temperaturein degree Celcius is %c, temp*1.8-32);

    }
    }
}
